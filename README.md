# SpinQuest Agentic MC Tuner

An agentic AI system that autonomously tunes Monte Carlo combinatoric background simulation for the **SpinQuest/E1039 Drell-Yan experiment** at Fermilab. A local LLM (Ollama/llama3) drives a ReAct loop to explore the parameter space, discovers the optimal tuning strategy, and produces a reweighted MC sample that closely matches experimental data.

---

## Results

| Metric | Value |
|--------|-------|
| KS statistic (full mass range) | **0.043** |
| KS statistic (low-mass [1.5–2.5] GeV/c²) | **0.044** |
| MC events processed | 17.5 million |
| Accepted events | 804,708 (4.6%) |
| Phase 1 runtime | ~32 min (one-time) |
| Phase 2 runtime | ~2 min per iteration |

Overlay of tuned MC vs experimental data across dimuon mass, rapidity, and individual muon momenta (generated by `overlay.py`):

> Run `conda run -n root_env python tuning/overlay.py` to regenerate plots.

---

## How It Works

### Agent Loop
The Ollama llama3 agent runs a **ReAct (Reason + Act)** loop, calling tools via JSON-structured outputs:

```
get_params → run_tuning → compute_ks → set_param → run_tuning → ...
```

The agent explores RATIO_CLIP_MAX, BDT_N_ESTIMATORS, BDT_LEARNING_RATE, EPOCHS, and ML cuts, tracking the best KS statistic in the low-mass region.

### Two-Phase Tuner (discovered through agent exploration)

```
Phase 1 — Normalizing Flow Rejection Sampling (runs once)
    ├── Train 6-layer affine coupling flows on 6D muon momenta (px, py, pz × 2 muons)
    ├── Stream all 17.5M MC events
    └── Accept with p = clip(r_momentum / NF_RATIO_CLIP, 0, 1)  →  ~2.3M saved

Phase 2 — BDT Mass Reweighting (fast, ~2 min per iteration)
    ├── Train GBReweighter on 100k MC vs all EXP events (asymmetric)
    ├── Stream only the 2.3M NF-accepted events
    ├── Accept with p = clip(w_mass / bdt_clip, 0, 1)
    └── Bisect bdt_clip to minimise KS(exp_mass, accepted_mass)
```

Phase 1 is cached to disk — subsequent runs skip it and go straight to Phase 2.

### Key Technical Choices
- **Normalizing flows** learn the 6D momentum density ratio p_exp / p_sim
- **GBReweighter** (hep_ml) corrects the 1D mass shape
- **Asymmetric BDT training**: 100k MC vs all ~950 EXP events — forcing equal sample sizes collapses performance when EXP statistics are limited
- **Midpoint bisection**: bdt_clip search starts at the midpoint of [0.5, 5.0]; starting at the edge caused the optimizer to miss the global minimum
- **Full mass range [1.5, 6.0] GeV** for flow training — narrowing to [1.5, 2.5] left too few EXP events and degraded NF quality

---

## Project Structure

```
spinquest-agentic-mc-tuner/
├── agent.py          # LLM ReAct agent (Ollama/llama3)
├── tools.py          # Agent tools: get_params, set_param, run_tuning, compute_ks, overlay
├── run_agent.sh      # Launch script
└── tuning/
    ├── rej_fast.py   # Two-phase tuner: NF RS → BDT mass RS (best result)
    ├── rej.py        # Combined NF+BDT adaptive tuner (reference, good for small datasets)
    ├── overlay.py    # Comparison plot generator
    └── data/
        ├── nf_accepted_comb.root          # Phase 1 output (cached, reusable)
        └── out_test_comb_momentum_adaptive.root   # Final tuned MC output
```

---

## Usage

### Run the agent (autonomous parameter exploration)
```bash
bash run_agent.sh --max-iter 5
# or with more iterations:
bash run_agent.sh --max-iter 10 --model llama3:latest
```

### Run the tuner directly (best config, no LLM needed)
```bash
# First run: Phase 1 (NF RS, ~32 min) + Phase 2 (BDT bisection, ~2 min)
conda run -n root_env python tuning/rej_fast.py

# Subsequent runs: Phase 1 is skipped automatically (nf_accepted_comb.root cached)
# Only Phase 2 runs — takes ~2 min
conda run -n root_env python tuning/rej_fast.py
```

### Generate overlay plots
```bash
conda run -n root_env python tuning/overlay.py
```

---

## Dependencies

| Package | Purpose |
|---------|---------|
| PyTorch | Normalizing flow training |
| hep_ml | GBReweighter (BDT mass reweighting) |
| uproot | Read/write ROOT files in Python |
| ROOT (PyROOT) | Streaming large ROOT trees for rejection sampling |
| scipy | KS statistic (ks_2samp) |
| ollama | Local LLM inference (llama3) |
| tqdm | Progress bars |

All dependencies are available in the `root_env` conda environment:
```bash
conda activate root_env
```

---

## Input Data

| File | Description |
|------|-------------|
| `exp_tagged_tgt_data_II.root` | Experimental dimuon data (SpinQuest target run II) |
| `mc_comb_muon_gun_march19.root` | Combinatoric MC — 17.5M muon-gun events |

Event selection: ML-based J/ψ, ψ' and Drell-Yan vetoes, vertex z > −600 cm, dimuon mass [1.5, 6.0] GeV/c².

---

## Physics Context

The SpinQuest experiment measures the Drell-Yan process to extract the sea quark spin asymmetry. The combinatoric background — random muon track combinations mimicking a dimuon signal — must be modelled accurately from MC simulation. This tuner reweights the MC momentum and mass distributions to match data, enabling a data-driven background subtraction in the low-mass region below the J/ψ peak (1.5–2.5 GeV/c²).
